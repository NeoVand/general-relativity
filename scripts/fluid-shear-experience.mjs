import {fluidDefaults} from '../web/fluid-shear-model.js';
import {fluidView,fluidReadout,fluidInsight,fluidSource} from '../web/fluid-shear-experience.js';
import {icon} from './scenes.mjs';
import {math} from './math-system.mjs';
export function fluidShearHTML(){
 const id='fluid-shear-lab',s=fluidDefaults,slider=(key,title,min,max,step,extra='')=>`<div ${extra}><div class="scene-parameter"><label for="${id}-${key}">${title}</label><output for="${id}-${key}" data-fl-value="${key}">${s[key]}</output></div><input type="range" id="${id}-${key}" data-fl-parameter="${key}" min="${min}" max="${max}" step="${step}" value="${s[key]}"/></div>`;
 return `<section id="${id}" class="spacetime-lab geometry-foundation fluid-shear-experience" data-fluid-shear aria-labelledby="${id}-title" data-narration-source="${fluidSource()}">
 <header class="scene-heading"><span class="eyebrow">${icon('CubeIcon')} MATTER LAB / 02</span><h2 id="${id}-title">How one layer pulls another.</h2><p data-fl-intro>Move the upper plate. The fluid beside it moves too, pulling the layers below. Slide the gold cut through the flow to measure the force between neighboring layers.</p></header>
 <div class="scene-viewbar" data-fl-controls data-no-narration hidden><div class="scene-view-switch" role="group" aria-label="What drives the flow"><button type="button" data-fl-mode="plate" aria-pressed="true">Moving plate</button><button type="button" data-fl-mode="pressure" aria-pressed="false">Pressure difference</button></div><div class="scene-actions"><button type="button" data-fl-reset title="Reset fluid experiment" aria-label="Reset fluid experiment">${icon('RefreshIcon')}</button></div></div>
 <div class="fl-legend"><span class="fl-key-velocity">Velocity and dye</span><span class="fl-key-shear">Shear force</span><span>Dye follows the fluid’s average motion.</span></div>
 <div data-fl-plot>${fluidView()}</div><p class="fl-scale-note">H = 2 cm · viewing window 12 cm long · height expanded to show the layers</p>
 <div class="scene-controls fl-controls" data-fl-controls data-no-narration hidden>${slider('drive','Upper plate speed [m/s]',-1.2,1.2,.02,'data-fl-drive')}${slider('gradient','Pressure drive',-1200,1200,20,'data-fl-gradient hidden')}${slider('viscosity','Viscosity [Pa·s]',.04,.3,.01)}${slider('probe','Cut height [H]',.1,.9,.01)}${slider('time','Time [s]',0,.8,.005)}<div class="scene-experiment-bar"><button type="button" class="scene-play" data-fl-play aria-pressed="false">Play</button><span>Slow motion · 0.1 physical seconds per second of playback</span></div></div>
 <div class="fl-readout" data-fl-readout>${fluidReadout()}</div><p class="gf-insight fl-insight" data-fl-insight>${fluidInsight()}</p>
 <details class="gf-method"><summary>Why force and momentum flux have opposite signs here</summary><div><p>Take x to the right and y upward. Focus on the fluid <strong>below</strong> the gold cut. If the fluid just above it moves faster to the right, it pulls the lower fluid to the right. The lower fluid exerts an equal leftward force on the upper fluid. These are the two gold arrows.</p><p>A Newtonian fluid’s shear force per area is proportional to the rate at which its velocity changes across layers:</p><div class="equation">${math(String.raw`\tau_{yx}=\mu\frac{dv_x}{dy}`,true,11)}</div><p>Here ${math(String.raw`\mu`,false,11)} is the <strong>dynamic viscosity</strong>, measured in pascal-seconds. One pascal is one newton per square metre. The first index names the cut’s normal direction; the second names the force direction. Positive ${math(String.raw`\tau_{yx}`,false,11)} means the upper fluid pushes the lower fluid toward +x.</p><p>That rightward momentum is entering the lower region <em>downward</em> through its top face. Its flux measured toward +y therefore has the opposite sign:</p><div class="equation">${math(String.raw`\Pi_{yx}=-\tau_{yx}`,true,11)}</div><p>No average fluid motion crosses this horizontal cut: ${math('v_y=0',false,11)}. The dye stays in its layer. The shear force nevertheless transfers momentum between layers through microscopic interactions. Those interactions are represented by the viscosity law; the dye dots are not a molecular simulation.</p><p>For a general slow Newtonian fluid, the spatial momentum flux separates into transport by bulk flow, pressure, and viscous stress:</p><div class="equation">${math(String.raw`\Pi_{ij}=\rho v_i v_j+p\delta_{ij}-\tau_{ij}`,true,11)}</div><p>The mass density is ${math(String.raw`\rho`,false,11)}. The first term carries momentum with moving material. The pressure term acts normally across a cut. The last term is the negative of the viscous force per area. Equivalently, the mechanical Cauchy stress is ${math(String.raw`\sigma_{ij}=-p\delta_{ij}+\tau_{ij}`,false,11)}. Compressive pressure has a negative mechanical-stress sign and a positive normal-momentum-flux sign.</p></div></details>
 <details class="gf-method"><summary>Derive the two velocity profiles</summary><div><p>The flow is steady and moves only along x. Neighboring layers are labeled by height y between plates at ${math('y=0',false,11)} and ${math('y=H',false,11)}. A thin layer receives a pressure force per volume ${math(String.raw`-dp/dx`,false,11)}. The difference between the shear forces on its top and bottom contributes ${math(String.raw`d\tau_{yx}/dy`,false,11)}. Newton’s law with zero acceleration gives</p><div class="equation">${math(String.raw`0=-\frac{dp}{dx}+\mu\frac{d^2v_x}{dy^2}`,true,11)}</div><p><strong>Moving plate.</strong> The pressure is constant. Thus ${math(String.raw`d^2v_x/dy^2=0`,false,11)}, so the profile is a straight line. No slip means the fluid matches each plate’s velocity: zero below and ${math('U',false,11)} above. Therefore</p><div class="equation">${math(String.raw`v_x(y)=U\frac yH,\qquad \tau_{yx}=\frac{\mu U}{H}`,true,11)}</div><p>This is <strong>Couette flow</strong>. Fixing the two plate speeds fixes the velocity profile. Increasing viscosity changes the force required to maintain it.</p><p><strong>Pressure difference.</strong> Both plates are stationary. Define ${math(String.raw`G=-dp/dx`,false,11)}, so positive G drives flow toward +x. Integrating the force-balance equation twice and imposing zero velocity at both walls gives</p><div class="equation">${math(String.raw`v_x(y)=\frac{G}{2\mu}y(H-y),\qquad \tau_{yx}=\frac G2(H-2y)`,true,11)}</div><p>This is <strong>plane Poiseuille flow</strong>. The speed is greatest at the middle, where the slope and shear vanish. Increasing viscosity at fixed pressure gradient reduces the speed. The shear profile remains fixed by the imposed pressure force. A constant pressure gradient is balanced by the change in shear from one layer to the next.</p></div></details>
 <details class="gf-method"><summary>What the animation represents</summary><div><p>This is an exact steady solution for an incompressible Newtonian fluid with constant viscosity, straight parallel plates, and no slip. Changing a control selects another steady solution; it does not simulate the transient while a flow starts. The plate separation is 2 cm and the visible window is 12 cm long. The drawing expands the height to make the layers readable.</p><p>Blue dots are passive dye markers following the calculated velocity. A repeating train of markers enters and leaves the viewing window. Short trails and blue arrows show velocity; the linked profile uses the same velocity calculation. The gold cut selects the same height in both views. Gold arrows show forces on the two adjacent fluid regions, not velocities.</p><p>Time is physical seconds, played at one tenth speed. The pressure-driven example uses a pressure of 100 Pa above an arbitrary constant background at the window’s center. These are gauge pressures: adding the same constant to every pressure does not change the pressure gradient or this classical steady flow. Its left and right pressures change consistently with the pressure gradient. The fluid model includes viscosity; a perfect-fluid stress tensor deliberately leaves that effect out.</p><p>Independent checks solve the steady force-balance equation on a grid with the stated wall velocities. Integrated viscous dissipation is checked against the work supplied by the moving wall or pressure gradient. The same classical solutions appear in <a href="https://www.damtp.cam.ac.uk/user/tong/fluids/fluids.pdf">David Tong’s fluid-mechanics notes</a> and <a href="https://www.damtp.cam.ac.uk/user/examples/D23Le.pdf">Cambridge’s Fluid Dynamics II notes</a>.</p></div></details>
 <span class="sr-only" role="status" data-fl-status data-no-narration></span></section>`;
}
