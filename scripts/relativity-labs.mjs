import {labRecords,defaultsFor} from '../web/lab-records.js';
import {labResultHTML} from '../web/relativity-labs.js';
const esc=value=>String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function relativityLabHTML(id,render){
 const record=labRecords[id],state=defaultsFor(id),r=source=>render(source,{id:`lab-${id}`,chapter:record.chapter}).html;
 return `<section class="relativity-lab" id="lab-${id}" data-relativity-lab="${id}" aria-labelledby="lab-${id}-title"><header><span class="eyebrow">CALCULATE · COMPARE · EXPLAIN</span><h4 id="lab-${id}-title">${esc(record.title)}</h4><p>${esc(record.question)}</p></header><p class="lab-prediction"><strong>Before you change anything</strong> ${esc(record.prediction)}</p><p class="lab-units">${esc(record.units)}</p><form data-lab-controls data-no-narration hidden aria-label="${esc(record.title)} controls">${record.parameters.map(p=>`<div class="lab-control"><label for="lab-${id}-${p.id}">${esc(p.label)} <output data-lab-value="${p.id}" for="lab-${id}-${p.id}">${esc(p.value)}</output></label>${p.options?`<select name="parameter-${p.id}" id="lab-${id}-${p.id}">${p.options.map(value=>`<option value="${value}" ${value===p.value?'selected':''}>${String(value).toUpperCase()}</option>`).join('')}</select>`:`<input type="range" name="parameter-${p.id}" id="lab-${id}-${p.id}" min="${p.min}" max="${p.max}" step="${p.step}" value="${p.value}">`}</div>`).join('')}</form><p data-lab-status role="status"></p><div data-lab-result data-no-narration>${labResultHTML(id,state)}</div><details class="lab-method"><summary>Equations, assumptions, and limits</summary>${record.equations.map(e=>`<div data-scientific-equation="${e.id}">${r(`$$${e.tex}$$`)}</div>`).join('')}<ul>${record.assumptions.map(a=>`<li>${esc(a)}</li>`).join('')}</ul><p>${esc(record.limitation)}</p><a href="${record.source.url}" target="_blank" rel="noopener">${esc(record.source.title)} ↗</a></details><div data-lab-actions data-no-narration hidden><button type="button" data-lab-reset>Reset the experiment</button><button type="button" data-lab-export>Export calculation · CSV</button><label for="lab-${id}-note">What did you predict? What changed your explanation?</label><textarea id="lab-${id}-note" data-lab-observation maxlength="4000" placeholder="Keep a claim, a measured result, and the assumption you would test next."></textarea><small>Settings and observations stay in your <a href="notebook.html">field notebook</a> in this browser. Export the notebook to keep a backup.</small></div></section>`;
}
