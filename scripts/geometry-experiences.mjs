import katex from 'katex';
import {icon} from './scenes.mjs';
import {manifoldFallback,orbitFallback} from '../web/geometry-experiences.js';
const math=(tex,role='')=>`<span class="gx-math ${role?'math-'+role:''}">${katex.renderToString(tex,{throwOnError:true,strict:false})}</span>`;
const narrations={
 manifold:'A smooth surface curves through three-dimensional space, but a point on it needs only two coordinates. The highlighted patch corresponds to the interior of a disk on the flat map. Move the rose point in that disk: the matching point moves over the surface. Two overlapping patches use different coordinate axes. Where both charts apply, switching the chart changes the pair of numbers but leaves the surface point unchanged. Outside a chart, the point still exists; that chart simply cannot name it. The optional tangent plane is the flat first-order approximation near the selected point. This graph is an example surface, not a drawing of four-dimensional spacetime.',
 precession:'Compare two orbits around the same central mass. The teal Newtonian ellipse returns to the same perihelion, the closest point to the Sun. The rose path has a small extra advance in its perihelion direction. Each completed radial cycle leaves a marker and a faint ray, so the changing direction can be compared. The physical relativistic advance is six pi times the Sun’s gravitational radius, divided by the orbital semi-major axis times one minus eccentricity squared. Mercury’s values give about one tenth of an arcsecond per orbit, or forty-three arcseconds per century. The reveal view magnifies only this extra rotation by three hundred thousand. It is a precessing-ellipse construction for the leading weak-field effect, not an exact strongly relativistic orbit. Changing eccentricity keeps the solar mass and Mercury’s orbital size fixed.'
};
export function geometryExperienceHTML(type,{reference=''}={}){
 if(!['manifold','precession'].includes(type))throw new Error(`Unknown geometry experience: ${type}`);
 const manifold=type==='manifold',id=manifold?'manifold-chart-experience':'orbital-precession-experience';
 const title=manifold?'A curved world. A flat address.':'An ellipse that remembers every revolution.';
 const deck=manifold?'Move one point between a surface and its map. Then change the map while the point stays put.':'Follow the closest approach. Newton brings it back to the same place; relativity turns its direction a little further.';
 const controls=manifold?`<div class="gx-controls" data-experience-controls data-no-narration><div class="gx-choice-group" role="group" aria-label="Choose a coordinate chart"><button type="button" data-gx-chart="a" aria-pressed="true"><i class="gx-key gx-key-a"></i>Chart A</button><button type="button" data-gx-chart="b" aria-pressed="false"><i class="gx-key gx-key-b"></i>Chart B</button></div><button type="button" data-gx-tangent aria-pressed="false">Tangent plane</button><button type="button" class="gx-icon-button" data-gx-reset aria-label="Reset point, chart and camera" title="Reset">${icon('RefreshIcon')}</button></div>`:
 `<div class="gx-controls" data-experience-controls data-no-narration><button type="button" class="gx-play" data-gx-play aria-pressed="false" aria-label="Play orbit animation"><span data-gx-playing="false">${icon('PlayIcon')}</span><span data-gx-playing="true" hidden>${icon('PauseIcon')}</span><span class="gx-button-label">Play</span></button><button type="button" data-gx-step>Next perihelion ${icon('ArrowRight01Icon')}</button><button type="button" class="gx-icon-button" data-gx-reset aria-label="Reset the orbit and camera" title="Reset">${icon('RefreshIcon')}</button><div class="gx-choice-group" role="group" aria-label="Angular magnification"><button type="button" data-gx-scale="1" aria-pressed="false">Actual scale</button><button type="button" data-gx-scale="300000" aria-pressed="true">Reveal the advance</button></div></div>`;
 const map=manifold?`<div class="gx-map-panel"><div class="gx-panel-heading"><span>The flat map</span>${math('(u,v)','geometry')}</div><div class="gx-domain" role="application" tabindex="0" aria-label="Move the point in the coordinate map. Drag, click, or use arrow keys. Home selects the overlap."></div><p class="gx-map-hint">Drag the point. Its partner moves on the surface.</p><div class="gx-map-shortcuts" data-experience-controls data-no-narration><button type="button" data-gx-point="overlap">On the overlap</button><button type="button" data-gx-point="only">Outside the other chart</button></div></div>`:'';
 const parameter=manifold?'':`<div class="gx-orbit-parameters" data-experience-controls data-no-narration><label class="gx-range"><span>Orbital shape <strong class="gx-e-value">${math('e=0.2056','geometry')}</strong></span><input type="range" data-gx-e min="0.05" max="0.7" step="0.0001" value="0.2056" aria-label="Orbital eccentricity" aria-valuetext="Eccentricity 0.2056"/><span class="gx-range-ends"><small>Nearly circular</small><small>More elongated</small></span></label><button type="button" data-gx-mercury>Mercury’s shape</button></div>`;
 const model=manifold?`<p>A <em>surface</em> is the curved object. A <em>chart</em> assigns two numbers to points in one open patch. Here the map ${math('F_A')} takes a pair of numbers back to the surface:</p><div class="gx-model-equation">${math(String.raw`F_A(u,v)=\bigl(u-0.55,\ v,\ h(u-0.55,v)\bigr)`,'geometry')}</div><p>The inverse chart simply reads ${math('(u,v)=(x+0.55,y)')}. The height is determined by those two values; it is not a third independent coordinate. Our smooth example is</p><div class="gx-model-equation">${math(String.raw`h(x,y)=0.38\sin(1.3x)\cos(1.15y)+0.16(x^2-y^2)`,'geometry')}</div><p>Chart B uses a shifted origin and axes rotated through ${math(String.raw`\pi/5`)}. On the overlap, the change of coordinates is an ordinary invertible rotation and translation:</p><div class="gx-model-equation">${math(String.raw`\begin{pmatrix}u'\\v'\end{pmatrix}=\begin{pmatrix}\cos\alpha&\sin\alpha\\-\sin\alpha&\cos\alpha\end{pmatrix}\left[\begin{pmatrix}u\\v\end{pmatrix}-\begin{pmatrix}1.1\\0.15\end{pmatrix}\right],\quad\alpha=\frac\pi5`,'transport')}</div><p>Each colored disk represents an <em>open</em> coordinate domain ${math(String.raw`u^2+v^2<1.35^2`)}; the rim marks its limiting boundary. The two derivatives of ${math('F_A')} are independent because their first two components are ${math('(1,0)')} and ${math('(0,1)')}. That is why two coordinates work smoothly even where the surface bends.</p><p>This particular graph also admits a global projection chart. We deliberately restrict two charts to explain overlap; we are not claiming every manifold has a global chart. The faded outer rim is a rendering window, not an edge of the mathematical surface. Coordinates and heights are dimensionless. This is a two-dimensional example embedded in Euclidean three-space, not a depiction of spacetime.</p><p>Background: <a href="https://www.math.toronto.edu/mgualt/courses/18-367/docs/DiffGeomNotes-5.pdf" target="_blank" rel="noopener">Gualtieri’s differential-geometry notes, coordinate charts and smooth maps</a>.</p>`:
 `<p>The <em>perihelion</em> is the closest approach to the Sun. A radial cycle runs from one perihelion to the next. To leading order in the weak gravitational field of a nonrotating spherical mass, its direction advances by</p><div class="gx-model-equation">${math(String.raw`\Delta\varpi=\frac{6\pi GM}{a(1-e^2)c^2}`,'curvature')}</div><p>Here ${math('a')} is the semi-major axis, ${math('e')} the eccentricity, and ${math('GM/c^2')} the gravitational radius. The model keeps ${math(String.raw`a=5.7909\times10^{10}\,\mathrm m`)} and ${math(String.raw`GM/c^2=1476.625\,\mathrm m`)} fixed at approximate Sun–Mercury values. For ${math('e=0.2056')}, the orbital period used for the century comparison is ${math(String.raw`87.969\,\text{days}`)}.</p><p>The rendered path is an explicitly approximate <em>precessing Kepler ellipse</em>. If ${math(String.raw`\chi`)} measures progress through successive radial cycles, we draw</p><div class="gx-model-equation">${math(String.raw`\frac ra=\frac{1-e^2}{1+e\cos\chi},\qquad\varphi=\left(1+\frac{A\Delta\varpi}{2\pi}\right)\chi`)}</div><p>${math('A=1')} shows the actual tiny advance; ${math(String.raw`A=300\,000`)} magnifies only its angular effect. This illustrates the leading accumulated precession and omits smaller periodic relativistic corrections to the radial motion. It does not solve an exact Schwarzschild geodesic. At the exaggerated setting it is a visual construction, not an orbit in a stronger physical field.</p><p>Animation uses Newtonian Kepler timing along the reference ellipse, with one radial cycle taking seven screen seconds. The star and planet markers are enlarged and are not to scale. Planetary perturbations, solar rotation and oblateness are omitted. The eccentricity control is restricted to ${math('0.05\leq e\leq0.7')}; a perfectly circular orbit has no distinguished perihelion.</p><p>Derivation and Mercury comparison: <a href="https://www.damtp.cam.ac.uk/user/tong/gr/grhtml/S1.html" target="_blank" rel="noopener">David Tong, General Relativity, perihelion precession</a>.</p>`;
 return `<section class="geometry-experience gx-${type}" data-geometry-experience="${type}" id="${id}" aria-labelledby="${id}-title"><div class="gx-topline"><span class="gx-eyebrow">${manifold?'Explore a manifold':'A precision test of gravity'}</span></div><h3 class="gx-title" id="${id}-title">${title}</h3><p class="gx-deck">${deck}</p>${controls}<div class="gx-visual-layout"><div class="gx-stage"><div class="gx-fallback">${manifold?manifoldFallback():orbitFallback()}</div><div class="gx-spatial" hidden></div><span class="gx-orbit-hint" data-no-narration>Drag to rotate</span>${manifold?'':`<div class="gx-scene-key" data-no-narration><span><i class="gx-key gx-key-a"></i>Newton</span><span><i class="gx-key gx-key-point"></i>GR</span></div><span class="gx-magnification" data-no-narration>Perihelion rotation ${math('\\times 300\\,000','observer')}</span>`}</div>${map}</div>${parameter}<div class="gx-readout" aria-label="Current measurements"></div><p class="gx-insight"></p>${manifold?'':'<p class="gx-mercury-result"></p>'}<details class="gx-model"><summary>Read the model and its limits ${icon('ArrowDown01Icon')}</summary><div>${model}</div></details>${reference?`<details class="gx-reference" data-no-narration><summary>Open the reference diagram</summary>${reference}</details>`:''}<p class="gx-live sr-only" role="status" aria-live="polite" data-no-narration></p><div class="narration-script" hidden>${narrations[type]}</div></section>`;
}
