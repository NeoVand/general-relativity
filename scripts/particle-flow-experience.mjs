import {particleFlowDefaults} from '../web/particle-flow-model.js';
import {particleFlowView,particleFlowReadout,particleFlowSource,particleFlowInsight,particleFlowMatrix,particleFlowCrossing} from '../web/particle-flow-experience.js';
import {icon} from './scenes.mjs';
import {math} from './math-system.mjs';
export function particleFlowHTML(){
 const id='particle-momentum-lab',s=particleFlowDefaults;
 const slider=(key,title,min,max,step,extra='')=>`<div ${extra}><div class="scene-parameter"><label for="${id}-${key}">${title}</label><output for="${id}-${key}" data-pf-value="${key}">${s[key]}</output></div><input type="range" id="${id}-${key}" data-pf-parameter="${key}" min="${min}" max="${max}" step="${step}" value="${s[key]}"/></div>`;
 return `<section id="${id}" class="spacetime-lab geometry-foundation particle-flow-experience" data-particle-flow aria-labelledby="${id}-title" data-narration-source="${particleFlowSource()}">
  <header class="scene-heading"><span class="eyebrow">${icon('CubeIcon')} MATTER LAB / 01</span><h2 id="${id}-title">What crosses the surface?</h2><p>Each moving particle carries energy and momentum. Put a detector through the box, choose what to count, and watch the crossings build a measurement.</p></header>
  <div class="scene-viewbar" data-pf-controls data-no-narration hidden><div class="scene-view-switch" role="group" aria-label="Choose the particle motion"><button type="button" data-pf-preset="stream" aria-pressed="true">One stream</button><button type="button" data-pf-preset="balanced" aria-pressed="false">Balanced motion</button><button type="button" data-pf-preset="slanted" aria-pressed="false">Tilted pairs</button></div><div class="scene-actions"><button type="button" data-pf-reset title="Reset particle experiment" aria-label="Reset particle experiment">${icon('RefreshIcon')}</button></div></div>
  <div class="pf-legend"><span class="pf-key-positive">Toward +x</span><span class="pf-key-negative">Toward −x</span><span class="pf-key-detector">Detector</span><span>96 particles · no collisions</span></div>
  <div class="pf-workbench"><div data-pf-plot>${particleFlowView()}</div><div class="pf-sidebar" data-pf-controls data-no-narration hidden>
   <div class="pf-choice"><span>Where to measure</span><div class="scene-view-switch" role="group" aria-label="Measurement direction">${['In the box','Across x','Across y','Across z'].map((x,i)=>`<button type="button" data-pf-row="${i}" aria-pressed="${i===1}"><span>${x}</span></button>`).join('')}</div></div>
   <div class="pf-choice"><span>What the particles carry</span><div class="scene-view-switch" role="group" aria-label="Carried quantity">${['Energy','p<sub>x</sub>','p<sub>y</sub>','p<sub>z</sub>'].map((x,i)=>`<button type="button" data-pf-column="${i}" aria-label="${['Energy','x-momentum','y-momentum','z-momentum'][i]}" aria-pressed="${i===1}"><span>${x}</span></button>`).join('')}</div></div>
   <div class="scene-controls pf-controls">${slider('speed','Particle speed [c]',.1,.85,.01)}${slider('plane','Detector position [L]',.15,.85,.01,'data-pf-plane')}${slider('angle','Stream angle',-Math.PI/2,Math.PI/2,Math.PI/180,'data-pf-angle hidden')}${slider('time','Counting time [L/c]',0,30,.05)}<div class="scene-experiment-bar"><button type="button" class="scene-play" data-pf-play aria-pressed="false">Play</button><span>Drag time to replay the same crossings.</span></div></div>
  </div></div>
  <div class="pf-readout" data-pf-readout>${particleFlowReadout()}</div><p class="pf-crossing" data-pf-crossing>${particleFlowCrossing()}</p><p class="pf-units">Readings use mc²/L³. L is the box width; m is one particle’s rest mass. One time unit is the time light takes to cross the box.</p>
  <p class="gf-insight pf-insight" data-pf-insight>${particleFlowInsight()}</p>
  <details class="gf-method"><summary>Why opposite motion can produce pressure</summary><div><p>First choose <strong>Balanced motion</strong>, <strong>Across x</strong>, and <strong>x-momentum</strong>. Half of the x-moving particles go right; the other half go left. Their average momentum is zero.</p><p>A right-moving particle carries positive x-momentum across the detector in the positive direction, giving a positive contribution. A left-moving particle carries negative x-momentum across it in the negative direction. Negative momentum transported backward also gives a positive contribution. Add both and divide by area and counting time: that is the <strong>normal momentum flux</strong>. For a gas with equal flux in every direction and no bulk motion, we call it pressure.</p><p>A reflecting wall gives the same reading: each collision reverses the particle’s normal momentum, so the wall receives twice that momentum, while only the population moving toward the wall hits it.</p><p>Now select Energy. Particle energies are positive on either trip, so the two crossing directions cancel in the long-time energy flux. Zero energy flow and positive pressure are compatible. Finally choose Tilted pairs and measure y-momentum across x: particles can transport momentum sideways to the detector’s normal. This is an off-diagonal flux. It comes from the prepared particle directions here; we have not modeled viscosity.</p></div></details>
  <details class="gf-method"><summary>Build the stress–energy tensor from these measurements</summary><div><p>Selecting where to measure chooses a <strong>row</strong>. Selecting the carried quantity chooses a <strong>column</strong>. The highlighted entry is the all-particle average corresponding to the detector. The first row records densities in the cube; the remaining rows record fluxes through surfaces.</p><div data-pf-matrix>${particleFlowMatrix()}</div><p>The time coordinate is ${math('x^0=ct',false,11)}. To give every entry energy-density units, the time row uses energy density and ${math('c',false,11)} times momentum density. The spatial rows use energy flux divided by ${math('c',false,11)} and ordinary momentum flux. These factors are already included in the readings.</p><p>In these box units, a particle has ${math(String.raw`p^0=\gamma`,false,11)} and ${math(String.raw`p^i=\gamma v^i`,false,11)}, with speed measured in units of ${math('c',false,11)}. Averaging the particles gives ${math(String.raw`T^{\mu\nu}=\sum_a p_a^\mu p_a^\nu/p_a^0`,false,11)} for unit box volume. The formula is symmetric in the two indices. In ordinary units the same expression is ${math(String.raw`T^{\mu\nu}=(c/V)\sum_a p_a^\mu p_a^\nu/p_a^0`,false,11)}, where ${math(String.raw`p_a^0=E_a/c`,false,11)}.</p></div></details>
  <details class="gf-method"><summary>The model, units, and counting checks</summary><div><p>All 96 particles have the same rest mass and speed. One stream has one shared velocity. Balanced motion contains equal populations along the six directions ±x, ±y, ±z; its average momentum vanishes and its three diagonal momentum fluxes agree. It is a discrete distribution with an isotropic second moment, not a calculation of thermal equilibrium. Tilted pairs contain equal opposing velocities in the xy plane.</p><p>The particles move freely. A particle leaving one side re-enters the opposite side, so the box samples an endlessly repeated medium. The detector is an internal plane spanning the cube, of area ${math('L^2',false,11)}. The drawing is an orthographic view of the three-dimensional positions. Short trails show velocity; a particle brightens briefly after a detector crossing. Fixed pseudorandom initial positions avoid arranging the two velocity directions in separate halves of the box. Changing a setup control recalculates the whole history from those same initial positions.</p><p>Every crossing contributes its carried momentum with the sign of its crossing direction. We divide the accumulated sum by area and elapsed time. For energy flux, divide by ${math('c',false,11)} as well before entering it in ${math('T',false,11)}. At zero time the crossing estimate is undefined, shown as a dash. For a short measurement, the crossing and all-particle estimates need not agree. A longer count reduces that sampling difference; no hidden smoothing forces agreement.</p><p>Energy is measured in ${math('mc^2',false,11)}, momentum in ${math('mc',false,11)}, distance in ${math('L',false,11)}, and time in ${math('L/c',false,11)}. Dividing the flux by its corresponding area and time units puts all tensor entries in ${math('mc^2/L^3',false,11)}. The ensemble construction follows <a href="https://web.mit.edu/edbert/GR/gr2b.pdf">Edmund Bertschinger’s treatment of a gas of noninteracting particles</a>.</p></div></details>
  <span class="sr-only" role="status" data-pf-status data-no-narration></span>
 </section>`;
}
